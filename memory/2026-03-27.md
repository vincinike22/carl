- 2026-03-27: Aurelius said he is going to a 24h hackathon, needs to tear down his home setup and rebuild it there, and asked to save everything so nothing gets lost.
- A full workspace git checkpoint was created before teardown: commit `f255866` with message `Checkpoint before hackathon teardown and rebuild`.
- Preservation/recovery note exists at `memory/2026-03-27-hackathon-setup-note.md`; on rebuild, check git log/status and review that note.
- Operational convention reinforced: use `memory/*.md` for raw daily notes, `MEMORY.md` for curated long-term memory, and `HEARTBEAT.md` only for short recurring operational checks.
- On this machine, no pre-existing standard personal SSH key was found in `~/.ssh`; a new Ed25519 SSH keypair was generated at `~/.ssh/id_ed25519` and `~/.ssh/id_ed25519.pub`.
- The user is trying to reconnect/setup a Hetzner server named `Moon` at IP `89.167.5.112`; connection depends on either the SSH key being authorized there or having the root password.
- The user wants an overview of what was brainstormed in Discord over the last few days, ideally across all channels in “Aurelius’s server”.
- Discord appears to already be connected in OpenClaw, but this session did not yet have direct readable Discord message context; identifying the relevant server as “Aurelius’s server” may be useful for the next attempt.
